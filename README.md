# Симулятор Доступності Вад Зору

Цей Python-скрипт аналізує веб-доступність шляхом симуляції різних вад зору на скріншоті веб-сторінки та оцінки, як ці вади впливають на читабельність тексту за допомогою OCR.

## 🌟 Основні Можливості

* **Автоматичний Скріншот:** Використовує Selenium для захоплення повного скріншота веб-сторінки.
* **Різноманітні Симуляції:** Моделює широкий спектр вад зору, зокрема:
    * Катаракта (легка та сильна)
    * Дальтонізм (Протанопія, Дейтеранопія)
    * Ахроматопсія (повна колірна сліпота)
    * Центральна Скотома (симптом AMD)
    * Тунельний Зір (симптом Глаукоми)
    * Метаморфопсія (спотворення)
    * "Мушки" (симптом Діабетичної Ретинопатії)
    * Втрата контрастної чутливості (CSF)
* **Аналіз Читабельності:** Використовує Tesseract OCR для розпізнавання тексту на оригінальному зображенні (базова лінія) та на кожному симульованому.
* **Візуальні Звіти:**
    1.  Генерує **індивідуальні зображення** для кожної симуляції з рамками, що показують:
        * <span style="color:green;">**Зелений:**</span> Правильно розпізнані слова, що збігаються з оригіналом.
        * <span style="color:red;">**Червоний:**</span> Хибно розпізнані слова ("артефакти"), яких не було в оригіналі.
    2.  Генерує **зведену таблицю-звіт** (`screenshot_report_readability.png`), що порівнює відсоток розпізнаних, втрачених слів та артефактів для кожної симуляції.

## 🛠️ Встановлення

### 1. Системні Залежності (Tesseract OCR)

Цей проект вимагає **Tesseract OCR**.

* **Windows:** Завантажте інсталятор з [Tesseract at UB Mannheim](https://github.com/UB-Mannheim/tesseract/wiki). Під час встановлення **обов'язково** додайте українську (`ukr`) та англійську (`eng`) мови.
* **macOS (Homebrew):** `brew install tesseract tesseract-lang`
* **Linux (Ubuntu/Debian):** `sudo apt-get install tesseract-ocr tesseract-ocr-ukr tesseract-ocr-eng`

### 2. Python Залежності

1.  Клонуйте репозиторій:
    ```bash
    git clone https://github.com/Glubin-yep/Accessibility_Simulator.git
    cd accessibility-simulator
    ```

2.  (Рекомендовано) Створіть та активуйте віртуальне середовище:
    ```bash
    python -m venv venv
    source venv/bin/activate  # macOS/Linux
    .\venv\Scripts\activate   # Windows
    ```

3.  Встановіть необхідні бібліотеки:
    ```bash
    pip install -r requirements.txt
    ```

## 🚀 Використання

Запустіть головний скрипт з вашого терміналу:

```bash
python main.py
```

Скрипт автоматично:
1.  Запустить Chrome у фоновому режимі (`headless`) для зняття скріншота (URL налаштовується в `config.py`).
2.  Збереже `screenshot_original.png`.
3.  Проаналізує оригінальний файл для створення базового набору слів.
4.  Запустить усі симуляції паралельно.
5.  Збереже результати кожної симуляції (наприклад, `screenshot_катаракта_(легка).png`).
6.  Згенерує фінальний звіт `screenshot_report_readability.png`.
7.  Виведе детальний звіт у консоль.

### Налаштування шляху до Tesseract

Якщо Tesseract встановлено не в системний `PATH`, ви можете вказати шлях до `tesseract.exe` вручну:

```bash
python main.py --tesseract_cmd "C:\Program Files\Tesseract-OCR\tesseract.exe"
```

(Або змініть шлях за замовчуванням безпосередньо у `main.py`).

## 📁 Структура Проекту

Проект розділено на модулі для легшої підтримки:

* `main.py`: Головний файл для запуску. Керує процесом, обробляє аргументи та виводить фінальний звіт у консоль.
* `config.py`: Містить основні налаштування: URL для аналізу та словник `SIMULATIONS`.
* `simulations.py`: Усі функції, що виконують математичні/візуальні перетворення зображень (симуляції вад).
* `color_utils.py`: Низькорівневі функції для роботи з кольором (гамма-корекція, матриці перетворення кольору).
* `analysis.py`: Логіка для роботи з Tesseract OCR (`get_word_data`).
* `reporting.py`: Логіка для генерації візуальних звітів (таблиця та індивідуальні зображення з рамками).
* `web_scraper.py`: Функція `take_screenshot` (логіка Selenium).
* `.gitignore`: Ігнорує згенеровані файли `*png`.
* `requirements.txt`: Список Python-залежностей.
